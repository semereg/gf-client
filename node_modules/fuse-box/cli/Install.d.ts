export declare class Install {
    constructor(args: any);
    private update();
    private pullRepository();
    private cloneRepository();
    private verifyRepository();
    private search(searchName?);
    private install(name, targetPath);
}
